export enum UploadDirectories {
  Base = 'uploads',
  Protected = 'protected',
  Public = 'public',
  image = 'images',
  video = 'videos',
  audio = 'audios',
}

export enum FileType {
  Image = 'image',
  Video = 'video',
  Audio = 'audio',
}
